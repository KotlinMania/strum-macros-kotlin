# Port report — `strum-macros-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `strum_macros`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 23 `use` sites from 3 other repos
- **Outbound**: 146 `use` sites to 3 other repos
- **Unresolved external**: 1 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `strum_macros` | `strum_macros` | 23 | 3 | COD-143 | 69 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (23 edges, 6 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-tui` | 10 | ✅ |
| `codex-kotlin` | `codex-protocol` | 7 | ✅ |
| `landlock-kotlin` | `landlock` | 2 |  |
| `strum-kotlin` | `strum` | 2 |  |
| `codex-kotlin` | `codex-app-server-protocol` | 1 | ✅ |
| `codex-kotlin` | `codex-otel` | 1 | ✅ |

### Symbol-level demand (8 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `strum_macros::Display` | 8 | 8 | 0 | 4 |
| `strum_macros::EnumIter` | 7 | 6 | 1 | 3 |
| `strum_macros::EnumString` | 3 | 3 | 0 | 1 |
| `strum_macros::AsRefStr` | 1 | 1 | 0 | 1 |
| `strum_macros::IntoStaticStr` | 1 | 1 | 0 | 1 |
| `strum_macros::$export` | 1 | 0 | 1 | 1 |
| `strum_macros::*` | 1 | 0 | 1 | 1 |
| `strum_macros::EnumCount` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 8 symbols)

##### `strum_macros::Display` (8 sites, 8 codex, 0 other, 4 caller crates)

Codex call sites:

- `codex-kotlin/codex-app-server-protocol` `src/protocol/common.rs:15` — `use strum_macros::Display`
- `codex-kotlin/codex-otel` `src/lib.rs:12` — `use strum_macros::Display`
- `codex-kotlin/codex-protocol` `src/config_types.rs:14` — `use strum_macros::Display`
- `codex-kotlin/codex-protocol` `src/openai_models.rs:13` — `use strum_macros::Display`
- `codex-kotlin/codex-protocol` `src/permissions.rs:14` — `use strum_macros::Display`
- `codex-kotlin/codex-protocol` `src/protocol.rs:61` — `use strum_macros::Display`
- `codex-kotlin/codex-tui` `src/bottom_pane/status_line_setup.rs:24` — `use strum_macros::Display`
- `codex-kotlin/codex-tui` `src/bottom_pane/title_setup.rs:15` — `use strum_macros::Display`

##### `strum_macros::EnumIter` (7 sites, 6 codex, 1 other, 3 caller crates)

Codex call sites:

- `codex-kotlin/codex-protocol` `src/config_types.rs:15` — `use strum_macros::EnumIter`
- `codex-kotlin/codex-protocol` `src/openai_models.rs:14` — `use strum_macros::EnumIter`
- `codex-kotlin/codex-protocol` `src/protocol.rs:13` — `use strum_macros::EnumIter`
- `codex-kotlin/codex-tui` `src/bottom_pane/status_line_setup.rs:25` — `use strum_macros::EnumIter`
- `codex-kotlin/codex-tui` `src/bottom_pane/title_setup.rs:16` — `use strum_macros::EnumIter`
- `codex-kotlin/codex-tui` `src/slash_command.rs:3` — `use strum_macros::EnumIter`

Other workspace call sites:

- `landlock-kotlin/landlock` `src/compat.rs:12` — `use strum_macros::EnumIter`

##### `strum_macros::EnumString` (3 sites, 3 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-tui` `src/bottom_pane/status_line_setup.rs:26` — `use strum_macros::EnumString`
- `codex-kotlin/codex-tui` `src/bottom_pane/title_setup.rs:17` — `use strum_macros::EnumString`
- `codex-kotlin/codex-tui` `src/slash_command.rs:4` — `use strum_macros::EnumString`

##### `strum_macros::AsRefStr` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-tui` `src/slash_command.rs:2` — `use strum_macros::AsRefStr`

##### `strum_macros::IntoStaticStr` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-tui` `src/slash_command.rs:5` — `use strum_macros::IntoStaticStr`

##### `strum_macros::$export` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `strum-kotlin/strum` `src/lib.rs:243` — `pub_use strum_macros::$export`

##### `strum_macros::*` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `strum-kotlin/strum` `src/lib.rs:236` — `pub_use strum_macros::*`

##### `strum_macros::EnumCount` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `landlock-kotlin/landlock` `src/compat.rs:12` — `use strum_macros::EnumCount as EnumCountMacro`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `proc-macro2-kotlin` | 25 | 4 | `proc-macro2` | COD-101 | 26 | To Do |
| `quote-kotlin` | 23 | 3 | `quote` | COD-105 | 30 | To Do |
| `syn-kotlin` | 98 | 29 | `syn` | COD-145 | 35 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `heck` | 7 | 7 | `strum_macros` |
