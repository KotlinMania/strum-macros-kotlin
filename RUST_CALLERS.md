# Rust callers — `strum-macros-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `strum_macros` | `strum_macros` | 23 | 6 | 3 | 19 |

## `strum_macros`

### Inbound symbols (8 distinct, 23 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `strum_macros`.

#### `strum_macros::Display` — 8 site(s) (8 codex, 0 other, 4 caller crates)

- ✅ `codex-kotlin/codex-app-server-protocol` `src/protocol/common.rs:15` — `use strum_macros::Display`
- ✅ `codex-kotlin/codex-otel` `src/lib.rs:12` — `use strum_macros::Display`
- ✅ `codex-kotlin/codex-protocol` `src/config_types.rs:14` — `use strum_macros::Display`
- ✅ `codex-kotlin/codex-protocol` `src/openai_models.rs:13` — `use strum_macros::Display`
- ✅ `codex-kotlin/codex-protocol` `src/permissions.rs:14` — `use strum_macros::Display`
- ✅ `codex-kotlin/codex-protocol` `src/protocol.rs:61` — `use strum_macros::Display`
- ✅ `codex-kotlin/codex-tui` `src/bottom_pane/status_line_setup.rs:24` — `use strum_macros::Display`
- ✅ `codex-kotlin/codex-tui` `src/bottom_pane/title_setup.rs:15` — `use strum_macros::Display`

#### `strum_macros::EnumIter` — 7 site(s) (6 codex, 1 other, 3 caller crates)

- ✅ `codex-kotlin/codex-protocol` `src/config_types.rs:15` — `use strum_macros::EnumIter`
- ✅ `codex-kotlin/codex-protocol` `src/openai_models.rs:14` — `use strum_macros::EnumIter`
- ✅ `codex-kotlin/codex-protocol` `src/protocol.rs:13` — `use strum_macros::EnumIter`
- ✅ `codex-kotlin/codex-tui` `src/bottom_pane/status_line_setup.rs:25` — `use strum_macros::EnumIter`
- ✅ `codex-kotlin/codex-tui` `src/bottom_pane/title_setup.rs:16` — `use strum_macros::EnumIter`
- ✅ `codex-kotlin/codex-tui` `src/slash_command.rs:3` — `use strum_macros::EnumIter`
- `landlock-kotlin/landlock` `src/compat.rs:12` — `use strum_macros::EnumIter`

#### `strum_macros::EnumString` — 3 site(s) (3 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/bottom_pane/status_line_setup.rs:26` — `use strum_macros::EnumString`
- ✅ `codex-kotlin/codex-tui` `src/bottom_pane/title_setup.rs:17` — `use strum_macros::EnumString`
- ✅ `codex-kotlin/codex-tui` `src/slash_command.rs:4` — `use strum_macros::EnumString`

#### `strum_macros::AsRefStr` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/slash_command.rs:2` — `use strum_macros::AsRefStr`

#### `strum_macros::IntoStaticStr` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/slash_command.rs:5` — `use strum_macros::IntoStaticStr`

#### `strum_macros::$export` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `strum-kotlin/strum` `src/lib.rs:243` — `pub_use strum_macros::$export`

#### `strum_macros::*` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `strum-kotlin/strum` `src/lib.rs:236` — `pub_use strum_macros::*`

#### `strum_macros::EnumCount` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `landlock-kotlin/landlock` `src/compat.rs:12` — `use strum_macros::EnumCount as EnumCountMacro`
