# Port targets — `strum-macros-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `strum_macros` that codex-rs uses, and therefore the minimum subset that `strum-macros-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-143] (rank `69`, tier `direct`, status `To Do`)

**Cargo package name**: `strum_macros`
**Rust module name (in `use` statements)**: `strum_macros`

## Summary

- **5** distinct symbols imported by codex-rs
- **19** total `use` statements
- **9** codex source files affected
- **4** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `strum_macros::Display`

- **8 `use` site(s)** across **4 codex member crate(s)**: `codex-app-server-protocol`, `codex-otel`, `codex-protocol`, `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-protocol/src/protocol/common.rs:15` (`codex-app-server-protocol`) — `use strum_macros::Display`
- `otel/src/lib.rs:12` (`codex-otel`) — `use strum_macros::Display`
- `protocol/src/config_types.rs:14` (`codex-protocol`) — `use strum_macros::Display`
- `protocol/src/openai_models.rs:13` (`codex-protocol`) — `use strum_macros::Display`
- `protocol/src/permissions.rs:14` (`codex-protocol`) — `use strum_macros::Display`
- `protocol/src/protocol.rs:61` (`codex-protocol`) — `use strum_macros::Display`
- `tui/src/bottom_pane/status_line_setup.rs:24` (`codex-tui`) — `use strum_macros::Display`
- `tui/src/bottom_pane/title_setup.rs:15` (`codex-tui`) — `use strum_macros::Display`

### `strum_macros::EnumIter`

- **6 `use` site(s)** across **2 codex member crate(s)**: `codex-protocol`, `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `protocol/src/config_types.rs:15` (`codex-protocol`) — `use strum_macros::EnumIter`
- `protocol/src/openai_models.rs:14` (`codex-protocol`) — `use strum_macros::EnumIter`
- `protocol/src/protocol.rs:13` (`codex-protocol`) — `use strum_macros::EnumIter`
- `tui/src/bottom_pane/status_line_setup.rs:25` (`codex-tui`) — `use strum_macros::EnumIter`
- `tui/src/bottom_pane/title_setup.rs:16` (`codex-tui`) — `use strum_macros::EnumIter`
- `tui/src/slash_command.rs:3` (`codex-tui`) — `use strum_macros::EnumIter`

### `strum_macros::EnumString`

- **3 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/bottom_pane/status_line_setup.rs:26` (`codex-tui`) — `use strum_macros::EnumString`
- `tui/src/bottom_pane/title_setup.rs:17` (`codex-tui`) — `use strum_macros::EnumString`
- `tui/src/slash_command.rs:4` (`codex-tui`) — `use strum_macros::EnumString`

### `strum_macros::AsRefStr`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/slash_command.rs:2` (`codex-tui`) — `use strum_macros::AsRefStr`

### `strum_macros::IntoStaticStr`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/slash_command.rs:5` (`codex-tui`) — `use strum_macros::IntoStaticStr`
